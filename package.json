{
  "name": "inference_dash",
  "version": "1.0.0",
  "description": "Simple Venice balances dashboard (in-memory)",
  "main": "server.js",
  "scripts": {
    "start": "NODE_ENV=production node server.js",
    "dev": "NODE_ENV=development node server.js",
    "build": "echo 'No build step required for this app'",
    "test": "mocha test/*.test.js",
    "test:watch": "mocha test/*.test.js --watch",
    "deploy": "vercel --prod",
    "deploy:preview": "vercel"
  },
  "keywords": [
    "dashboard",
    "balances",
    "inference"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "basic-auth": "^2.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "mocha": "^10.0.0"
  }
}
